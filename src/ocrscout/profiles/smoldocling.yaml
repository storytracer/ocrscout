name: smoldocling
source: docling
model_id: ds4sd/SmolDocling-256M-preview
output_format: doctags
normalizer: doctags
has_bboxes: true
has_layout: true
model_size: 256M
backend_args: {}
tier: curated
metadata:
  notes: |
    Ultra-compact (~256M) VLM. Run via the full Docling pipeline (which
    natively supports SmolDocling as a VLM stage) rather than the upstream
    uv-scripts/ocr smoldocling-ocr.py. Requires the `docling` extra.
