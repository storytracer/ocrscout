"""HuggingFace Hub publishing for ocrscout runs.

* :mod:`ocrscout.publish.dataset` — push a run as a dataset repo.
* :mod:`ocrscout.publish.space` — push a Gradio Space that views the dataset.
* :mod:`ocrscout.publish._stats` — pure aggregation helpers reused by the
  dataset card renderer and by the viewer.
"""
